import styled from 'styled-components';

const UpdateButton = styled.div`
  border: none;
  opacity: 0;
  visibility: hidden;
  display: block;
  transition: all 0.2s;
  cursor: pointer;
`;

export default UpdateButton;
